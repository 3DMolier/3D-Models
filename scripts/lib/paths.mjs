/*
 * paths.mjs - корень проекта, вычисленный, а не вписанный.
 *
 * ЗАЧЕМ. В 117 скриптах стояла строка
 *   const ROOT = 'D:/3d/документы/Blogger/Clode_and_Gpt_Website';
 * то есть путь на конкретном компьютере, с кириллицей в середине. Любая
 * сборка не с этой машины - в Actions, на другом диске, после переименования
 * папки - падала бы на первом же чтении файла. Переехать было бы нельзя без
 * правки сотни файлов.
 *
 * КАК. Модуль знает, где лежит он сам (import.meta.url), а лежит он всегда в
 * scripts/lib/ - значит корень ровно двумя уровнями выше. Ни настроек, ни
 * переменных окружения, ни зависимости от того, откуда запущен скрипт: из
 * корня, из scripts/ или из любой другой папки - ответ один и тот же.
 *
 * Разделитель приводим к косой черте: пути потом склеиваются со строками
 * вида '/models/' и попадают в сравнения, а обратная косая на Windows дала бы
 * два разных написания одного пути.
 */
import path from 'node:path';
import { fileURLToPath } from 'node:url';

export const ROOT = path
  .resolve(fileURLToPath(import.meta.url), '..', '..', '..')
  .split(path.sep).join('/');

export default ROOT;
